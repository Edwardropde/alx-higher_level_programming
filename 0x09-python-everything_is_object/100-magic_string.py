#!/usr/bin/python3
# Define a function called magic_string
def magic_string():
    # Initialize magic_string.counter if it doesn't exist and set to -1
    string += ["BestSchool"]
    # Return the string "BestSchool" repeated a number of times
    # Each repetition is separated by a comma
    return ", ".join(string)
